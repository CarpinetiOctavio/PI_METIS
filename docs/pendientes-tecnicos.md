# Deuda técnica — registro vivo

Registro de lo que sabemos que falta y no tiene otro archivo dónde vivir:
no es una decisión ya tomada (`docs/decisiones/`), no es una pregunta de
dominio para escalar a Facundo o Carlos (`docs/auditoria/pendientes/`), y no
es parte de un plan de trabajo activo que se borra al cerrarse. Creado en el
Bloque 0 del plan de implementación de Etapa 2 (09/08/2026) porque los tres
directorios de test vacíos del proyecto no tenían dónde registrarse — y por
eso se perdían.

**Regla:** cada entrada dice qué falta, qué bloquea, y quién la cierra. Las
entradas que se cierran se tachan con la fecha de cierre, no se borran —
mismo criterio de trazabilidad que rige el resto del proyecto (ver
`docs/decisiones/README.md`, `docs/historico/README.md`).

| Pendiente | Qué bloquea | Quién lo cierra | Estado |
|---|---|---|---|
| ~~`tests/integration/` vacío (solo `__init__.py`)~~ | Ningún test de integración corre en CI | Bloque A6 del plan de Etapa 2 lo estrena | **Cerrado 09/08/2026** — `test_etapa2_stream_distribution_decision.py` |
| `tests/regression/` vacío | Ningún test de regresión matemática corre en CI | Lo lleva Octavio por su lado — retirado del plan de Etapa 2 (Kevin, 09/08/2026), §6 queda como insumo para él | Abierto — sin dueño de este frente |
| `tests/e2e/` vacío | `constraints.md` excluye E2E de UI del scope V1.0, pero los E2E de API que `testing.md` §3 compromete no están excluidos y tampoco existen | Sin plan asignado todavía | Abierto |
| El job `test` de CI tolera exit code 5 | Un job de CI en verde no distingue "sin tests que correr" de "tests corridos y en verde" para integration/regression | Deja de hacer falta cuando `tests/integration/` tenga tests reales; se quita en un PR propio, no junto con el que agrega los tests | Abierto |
| ~~FE-16 — reescrita 12/08/2026, el diagnóstico original era incorrecto~~ | La afirmación original («`Etapa1Result` no expone la serie cruda») dejó de ser cierta el 12/08/2026 con el PR 8 del plan de Etapa 2 (DECISIÓN 057) — `Etapa1Result` ya tiene `serie_efectiva`/`timestamps_efectivos`. El bloqueo real, verificado contra el código, eran tres huecos distintos: (1) `_serializar_etapa1()` no incluía `serie_efectiva` ni `timestamps_efectivos` en `result_etapa1` ni en `analysis_results.etapa1`; (2) `test_result_dict()` no serializaba `indice_atipico`, ya presente en `TestResult`; (3) la serie mensual cruda y sus timestamps no viajaban a ningún lado — `analyses` no tenía columna de timestamps y `GET /history/{id}` no devolvía `serie` ni `configuracion`. Bloqueaba la serie temporal, el boxplot mensual y el gráfico de Chow | Los 5 PRs del plan de cierre de pendientes no-test (`docs/plan-cierre-pendientes-no-test.md`, borrado tras este cierre), sobre la base de [DECISIÓN 058](decisiones/decision058.md) | **Cerrado 12/08/2026** — PR 3 (#53) agregó el bloque `datos` completo a `result_etapa1`/`analysis_results.etapa1`, `indice_atipico` en `test_result_dict()`, la columna `analyses.timestamps` (migración `005`) y `serie`/`timestamps`/`configuracion` en `GET /history/{id}`; PR 4 (#54) montó `Etapa1SerieTemporalChart`/`Etapa1ChowChart` (`InteractiveChart` ganó `xScale="linear"`); PR 5 (#55) montó `Etapa1BoxplotMensualChart` (componente `BoxPlot` propio) y conectó `HistoryDetailPage` (nota de criterio de año + estado vacío explícito para análisis anteriores a la migración). Verificado en el navegador de dev contra el backend real en los tres PRs, no solo con tests |
| ~~`resolucion_temporal` se calcula y nunca se consume~~ | Una serie mensual entra al pipeline y se analiza como si fuera anual, sin error ni warning | Bloque F4 del plan de Etapa 2 (requiere `agregar_a_maximos_anuales()`) | **Cerrado 12/08/2026** — `agregar_a_maximos_anuales()` se llama al principio de `ejecutar_etapa1()` cuando `resolucion_temporal=="mensual"` (DECISIÓN 057) |
| ~~`_espaciado_regular()` da falso positivo en toda serie mensual~~ | Cualquier serie mensual real dispara `CONTRACT_IRREGULAR_SPACING` (meses de 28/30/31 días) | Bloque F2.2 del plan de Etapa 2 | **Cerrado 11/08/2026** — comparación por ordinal de mes (`PeriodIndex.asi8`) en vez de delta exacto en días cuando `resolucion_temporal == "mensual"`. De paso se encontró y cerró un segundo bug relacionado: con timestamps de texto (CSV sin `parse_dates`), la resta levantaba `TypeError` y el chequeo se saltaba en silencio para cualquier serie con fechas de texto, irregular o no — el fix nuevo cubre ambos casos con la misma rama |
| ~~`_inferir_resolucion()` usa el promedio y no la moda de los deltas~~ | Una serie mensual con un hueco largo puede inferirse como `"anual"` sin que nada lo note | Bloque F2.3 del plan de Etapa 2 | **Cerrado 11/08/2026** — `parser.py::_inferir_resolucion()` usa la moda de los deltas, no el promedio de extremos |
| ~~MSW sin uso real si el Bloque B del plan de Etapa 2 borra su último handler~~ | Dependencia de dev mantenida sin ningún caso de uso real en el repo | Evaluar sacarlo del proyecto en el mismo PR que borra el último handler | **Cerrado 09/08/2026** — `msw` salió de `package.json` por completo en el Bloque B (ver DECISIÓN 042, addendum) |
| ~~`schemas/analysis.py::AnalysisRequest` código muerto~~ | Modelo Pydantic que ninguna ruta importa — mantenerlo invita a que alguien lo cablee a medias de nuevo (mismo patrón que produjo DECISIÓN 037) | Se resuelve en A3 del plan de Etapa 2 (cablear como modelo real o borrar) | **Cerrado 09/08/2026** — borrado (DECISIÓN 054) |
| ~~`pi_metis-backend-1` corre `uvicorn` sin `--reload`~~ | `docker cp` actualiza los archivos del contenedor, pero el proceso ya arrancado sigue sirviendo el código que tenía cargado en memoria — solo un `docker restart pi_metis-backend-1` hace que el servidor HTTP vea cambios de código. `pytest`/`ruff` vía `docker exec` no lo sufren (procesos nuevos, importan fresco); solo lo sufre verificar contra el servidor ya corriendo (navegador, `curl` manual). Encontrado en el Bloque C del plan de Etapa 2 (11/08/2026): un smoke test en navegador contra el backend real venía fallando con `curva_ajuste`/`puntos_empiricos` ausentes del payload pese a que el código en disco del contenedor ya los tenía — diagnosticado comparando `docker exec ... python3 -c "..."` (proceso nuevo, código correcto) contra el `curl` directo al servidor HTTP corriendo (proceso viejo, código viejo) | PR `fix/backend-hot-reload`, del plan de cierre de pendientes no-test | **Cerrado 12/08/2026** — `docker-compose.yml`, servicio `backend`: `command: uvicorn ... --reload` + `volumes: ["./backend:/app"]` (`uvicorn[standard]==0.29.0` ya traía `watchfiles`, no hizo falta dependencia nueva). Verificado editando `metis/main.py` en el host sin `docker cp` ni `docker restart`: `WatchFiles detected changes... Reloading` en los logs y el cambio reflejado en `curl` de inmediato |
| `docker-compose.yml` base sirve el código del host en `backend`, no el de la imagen | El mismo compose es la aproximación al despliegue de la UCC (`architecture.md`) — con bind mount + `--reload`, un `docker compose up` con este archivo no reproduce lo que correría en producción real | Evaluar cuando aparezca un compose de producción real (M4/M5): mover `--reload`/`volumes` a un `docker-compose.override.yml` y dejar el base con el `CMD` del Dockerfile | Abierto — no urgente, sin compose de producción todavía |
